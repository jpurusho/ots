# Only needed for browser-only dev (npm run dev).
# Electron builds use ~/.ots/config.json instead — configured via Setup Wizard.
#
# To auto-generate from local Supabase: bash scripts/setup-env.sh

VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_BOOTSTRAP_ADMIN=your-email@gmail.com
