name: Build & Release Electron App + Deploy Web

# Single workflow for releases:
# 1. Builds macOS Electron .app with bundled Python backend
# 2. Creates GitHub Release with .zip
# 3. Deploys web app to GitHub Pages

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Node dependencies
        run: npm ci

      - name: Build app
        run: make build
        env:
          ELECTRON_BUILD: 'true'

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: OTS-mac
          path: release/*.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.zip
          generate_release_notes: true

      - name: Trigger web deployment
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          gh workflow run deploy.yml --ref main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

